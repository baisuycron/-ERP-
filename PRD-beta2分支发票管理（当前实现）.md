# PRD：beta2 分支发票管理（当前实现）

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档名称 | beta2 分支发票管理（当前实现）PRD |
| 所属分支 | `beta2` |
| 整理依据 | `D:\闪电帮帮\beta2-git\src\App.jsx` 当前前端实现 |
| 适用对象 | 产品、设计、前端、后端、测试 |
| 文档性质 | 基于当前分支实际页面和交互整理，不等同于未来规划版需求 |
| 更新时间 | 2026-05-30 |

## 2. 产品概述

当前 `beta2` 分支中的发票管理，核心是围绕“订单维度开票申请”开展的供应商后台管理能力，覆盖以下场景：

- 发票申请列表管理
- 待开票订单的单笔/批量确认开票
- 待开票订单的单笔/批量驳回
- 已开票订单的单笔/批量修改发票
- 发票详情查看
- 历史操作追踪
- 发票类型开关设置
- B2B 批量上传发票与合并开票子页面

同时，当前分支还体现了与买家侧、平台侧的协同关系：

- 买家侧可查看已开票详情、预览/下载电子发票
- 平台侧存在独立审核视角，可查看、确认、驳回、修改与追踪记录

## 3. 产品目标

- 让供应商可按订单快速处理开票申请
- 将开票状态、售后状态、开票金额、发票主体等信息放在同一工作台统一管理
- 支持批量处理，降低人工逐单开票成本
- 保证批量开票、批量上传等高风险动作具备一致性校验和失败拦截
- 保留历史操作链路，便于追责和售后协同

## 4. 页面入口与信息架构

### 4.1 供应商后台入口

`供应商后台 > 店铺 > 发票管理`

### 4.2 页面页签

当前实现包含以下页签/子页：

- `发票管理`
- `发票信息`
- `订单信息`
- `历史操作`
- `批量上传发票`
- `发票设置`

其中：

- `发票信息`、`订单信息`、`历史操作` 为从列表进入的详情型页签
- `批量上传发票` 为发票管理下级工具页
- `发票设置` 为发票管理主页内状态页签之一

### 4.3 发票管理状态页签

列表页按开票状态区分为：

- `全部`
- `待开票`
- `已驳回`
- `已撤销`
- `已开票`
- `发票设置`

说明：

- 除 `全部`、`已开票`、`发票设置` 外，页签展示对应数量
- 平台审核视角不展示 `发票设置`

## 5. 角色与职责

| 角色 | 当前能力 |
| --- | --- |
| 供应商/店铺运营 | 查询、筛选、查看详情、确认开票、驳回、修改发票、批量导入发票、维护发票设置 |
| 买家 | 在订单详情查看开票结果、查看发票详情、预览/下载 PDF |
| 平台运营 | 独立审核视角查看和处理发票申请，不包含发票设置 |

## 6. 核心数据口径

### 6.1 当前业务主体

当前分支以“订单维度开票申请”作为核心对象，列表中的每一行对应一个订单的开票申请记录。

### 6.2 当前页面主字段

列表页当前实现包含以下核心字段：

- 订单号
- 店铺信息
- 发票类型
- 付款方式
- 发票抬头
- 纳税人识别号
- 订单总额
- 售后状态
- 售后金额总计
- 申请开票金额
- 发票应开金额
- 发票金额（含税）
- 发票内容
- 需要单独开票
- 开票批次
- 开票备注
- 买家账号
- 闪购门店
- 支付时间
- 申请时间
- 开票时间
- 发票号码
- 开票方式
- 开票状态
- 是否过售后期

### 6.3 历史记录类型

当前实现中的历史操作类型包括：

- 申请开票
- 修改开票申请
- 撤销开票申请
- 重新提交开票申请
- 驳回开票申请
- 开票完成
- 修改发票

## 7. 功能说明

### 7.1 列表查询与筛选

当前列表支持按以下条件筛选：

- 订单号
- 申请时间
- 开票状态
- 发票内容
- 支付时间
- 售后状态
- 需要单独开票
- 开票时间
- 发票号码
- 发票抬头
- 纳税人识别号
- 店铺信息
- 付款方式
- 买家账号
- 闪购门店
- 开票批次

筛选特征：

- 店铺信息支持店铺名称/店铺 ID 模糊查询
- 闪购门店支持门店名称/门店 ID 模糊查询
- 发票抬头、买家账号、开票批次支持关键字匹配
- 日期字段均为区间筛选
- 支持“查询”“重置”

### 7.2 标识筛选

仅在 `全部` 和 `待开票` 页签下展示标识筛选，支持：

- `全部`
- `即将超时`
- `超时`
- `已修改`
- `撤销重提`

标识口径：

- 即将超时：申请时间起算，距 5 天超时阈值剩余 1 天内
- 超时：申请时间起算已超过 5 天
- 已修改：`修改时间` 与 `申请时间` 不一致
- 撤销重提：同时存在“撤销时间”和“重新提交时间”

### 7.3 列表页批量工具栏

当前实现支持以下批量动作：

| 场景页签 | 可见动作 |
| --- | --- |
| 全部 | 批量确认开票、批量修改开票、批量驳回 |
| 待开票 | 批量确认开票、批量驳回 |
| 已开票 | 批量修改开票 |

说明：

- 批量能力仅供应商视角可用
- 平台视角不展示勾选列和供应商批量工具栏

### 7.4 列设置

列表页支持列设置能力：

- 字段显示/隐藏
- 左侧冻结
- 右侧冻结
- 拖拽排序
- 一键重置

固定约束：

- `操作` 列固定在右侧
- 复选列固定在左侧
- `操作` 列不可取消冻结

### 7.5 发票详情页

发票详情页当前分为以下信息区块：

- 发票信息
- 抬头信息
- 备注信息
- 收票信息
- 订单信息
- 商品清单

发票信息区展示：

- 开票状态
- 需要单独开票
- 发票类型
- 发票内容
- 申请时间
- 发票号码
- 开票金额（含税）
- 开票金额（不含税）
- 开票时间

明细页能力：

- 已开票且存在发票号时支持 `预览发票`
- 已开票且存在发票号时支持 `下载发票`
- 已驳回时可展示驳回原因提示卡

### 7.6 历史操作页

历史操作页支持按时间轴查看记录，并支持从“申请开票”记录中点击开票批次回查列表。

回查动作结果：

- 清空当前详情态
- 自动切回 `全部`
- 自动带入 `开票批次` 筛选条件
- 自动重置标识筛选为 `全部`

### 7.7 发票设置

发票设置页当前支持维护可开票类型：

- 电子普通发票
- 电子增值税专用发票

页面提示文案为：

- 应平台合规要求，发票税点需包含在商品价格中

保存约束：

- 至少勾选一种发票类型

保存结果：

- 成功提示“发票设置已保存”

### 7.8 单笔/批量确认开票

确认开票弹窗包含三部分：

- 汇总信息
- 附件上传
- 发票信息填写

汇总信息展示：

- 发票类型
- 发票抬头
- 发票内容
- 订单总额
- 售后金额总计
- 申请开票金额
- 发票应开金额

表单字段：

- 发票附件
- 发票号码
- 开票金额（含税）
- 开票金额（不含税）
- 开票时间

上传限制：

- 仅支持 PDF
- 大小不超过 5M
- 最多上传 1 份

提交成功后：

- 开票状态变更为 `已开票`
- 开票方式写为 `手动`
- 发票号写入记录
- 开票时间写入记录
- 历史操作新增“开票完成”

### 7.9 单笔/批量驳回

驳回弹窗支持：

- 输入驳回原因
- 100 字以内
- 给出“建议先与买家沟通”提示

提交成功后：

- 开票状态变更为 `已驳回`
- 记录驳回时间
- 记录驳回原因
- 历史操作新增“驳回开票申请”

### 7.10 单笔/批量修改发票

仅 `已开票` 记录可修改发票。

修改弹窗字段与确认开票一致：

- 发票附件
- 发票号码
- 开票金额（含税）
- 开票金额（不含税）
- 开票时间

提交成功后：

- 保持开票状态为 `已开票`
- 更新发票号、开票金额、开票时间
- 开票方式写为 `手动`
- 历史操作新增“修改发票”

### 7.11 批量上传发票

批量上传发票是发票管理的下级子页面，当前实现包含四块能力：

- 三步操作引导
- ZIP 上传导入
- 导入结果查看
- 导入任务进度查询

#### 7.11.1 上传说明

上传要求：

- 上传文件为 `.zip`
- ZIP 内需包含发票 PDF 和订单映射 Excel
- PDF 必须按 `发票号码.pdf` 命名
- Excel 需包含两个 Sheet：`发票信息`、`订单关联`

支持动作：

- 下载模板
- 选择 ZIP
- 导入

#### 7.11.2 导入结果

导入完成后展示：

- 导入批次号
- 上传时间
- 成功发票数
- 失败发票数
- 发票级结果列表
- 导出失败明细

结果列表字段：

- 发票号码
- PDF 文件名
- 关联订单数
- 导入状态
- 失败原因

#### 7.11.3 发票详情（二级）

导入结果中的单张发票可查看详情，包括：

- 发票基础信息
- PDF 文件信息
- 关联订单列表
- 操作日志

导入成功后支持：

- 修改开票信息
- 替换 PDF

明确限制：

- 不支持修改关联订单
- 如需调整订单关系，需修正 `mapping.xlsx` 后重新导入

#### 7.11.4 任务进度

任务进度列表支持按以下条件筛选：

- 任务状态
- 操作人员
- 操作时间

任务状态包括：

- 执行中
- 全部成功
- 全部失败
- 部分失败

当前任务列表支持：

- 查询
- 重置
- 分页
- 对失败任务下载失败数据

## 8. 核心业务规则与逻辑限制

### 8.1 状态与动作矩阵

| 开票状态 | 可执行动作 |
| --- | --- |
| 待开票 | 发票详情、历史操作、确认开票、驳回 |
| 已开票 | 发票详情、历史操作、修改发票 |
| 已驳回 | 发票详情、历史操作 |
| 已撤销 | 发票详情、历史操作 |

### 8.2 售后规则

当前实现明确体现以下售后逻辑：

- 任一 SKU 处于售后中时，不允许申请开票
- 部分退款订单仍可进入发票管理，但需结合最终开票金额处理
- 若整单均退款成功，则发票管理页面不展示该订单
- 售后状态聚合后展示为：售后中、部分退款、售后关闭等业务标签

售后中状态口径包含：

- 待供应商审核
- 待买家寄货
- 待供应商收货
- 待平台确认
- 退款中
- 售后审核中

售后关闭口径包含：

- 供应商拒绝
- 平台驳回
- 买家取消

### 8.3 开票超时规则

- 以 `申请时间` 为起点计算
- 超时阈值为 5 天
- 提前 1 天进入“即将超时”
- 支持在列表通过标识筛选查看

### 8.4 批量确认开票一致性限制

批量确认开票前必须同时满足：

- 至少选择 1 条记录
- 所选记录全部为 `待开票`
- 所选记录中不能混入 `已开票`
- 所选记录发票抬头一致
- 所选记录纳税人识别号一致
- 所选记录发票类型一致
- 所选记录发票内容一致

不满足时，当前实现直接拦截并提示，不允许进入批量开票弹窗。

### 8.5 批量驳回限制

- 至少选择 1 条记录
- 所选记录必须全部为 `待开票`
- 混入其他状态时直接拦截

### 8.6 批量修改限制

- 至少选择 1 条记录
- 所选记录必须全部为 `已开票`
- 混入其他状态时直接拦截

### 8.7 表单必填规则

确认开票、修改发票当前必填项为：

- 发票附件
- 发票号码
- 开票金额（含税）
- 开票时间

当前非必填项：

- 开票金额（不含税）

驳回当前必填项为：

- 驳回原因

### 8.8 发票明细显示规则

- `待开票 / 已驳回 / 已撤销` 状态下，列表中的“发票金额（含税）”显示为 `-`
- 未开票时，发票详情中的 PDF 预览/下载按钮不展示
- 已开票且存在发票号时，才允许预览/下载 PDF

### 8.9 抬头信息显示规则

- 个人抬头不展示纳税人识别号扩展字段
- 普通发票默认隐藏注册地址、电话、开户行、银行账号等扩展字段
- 专票且非个人抬头时展示完整企业抬头信息

### 8.10 已修改提示规则

若买家曾修改开票申请，则详情页会给出提示：

- 发票类型已修改时，展示原发票类型与修改时间
- 发票抬头已修改时，展示原发票抬头与修改时间

### 8.11 已驳回/已撤销提示规则

- 已驳回：展示驳回日期与驳回原因
- 已撤销：展示买家撤销时间

### 8.12 开票金额规则

当前页面文案定义：

- 售后金额总计 = 售后中金额 + 已退款金额
- 发票应开金额 = 订单总额 - 售后金额总计

同时页面保留以下金额口径：

- 订单总额
- 申请开票金额
- 发票应开金额
- 开票金额（含税）
- 开票金额（不含税）

### 8.13 发票设置规则

- 发票税点需包含在商品价格中
- 至少保留一种发票类型，否则不可保存

## 9. 批量上传发票专项规则

### 9.1 导入单位

- 系统按“发票”维度处理导入，不按订单维度逐条成功/失败

### 9.2 合并开票规则

同一张发票关联多笔订单时，全部订单需同时满足：

- 发票抬头一致
- 纳税人识别号一致
- 发票类型一致
- 同属一个店铺

### 9.3 原子性规则

- 同一张发票不允许部分成功、部分失败
- 任一关联订单校验失败，则整张发票导入失败
- 整票失败时，不生成发票记录
- 整票失败时，不生成订单关联
- 整票失败时，不更新订单开票状态

### 9.4 导入后可修改范围

导入成功后仅允许：

- 修改发票基础信息
- 替换 PDF 文件

导入成功后不允许：

- 修改关联订单

## 10. 当前非范围与实现边界

当前分支已明确或实际体现的边界如下：

- 一个订单只能对应一张发票，不支持拆票
- 当前主体仍是订单维度申请，未体现独立“开票申请单”实体
- 批量导入成功后，不支持在线调整订单关联关系
- PDF 命名依赖发票号，不支持系统自动纠错文件名
- `导出查询结果` 按钮当前已展示，但在当前分支中未见明确的实际处理逻辑，可视为预留能力

## 11. 建议测试重点

- 状态页签切换与数量统计
- 各筛选项单独/组合查询
- 标识筛选口径：即将超时、超时、已修改、撤销重提
- 待开票、已开票、已驳回、已撤销的动作可见性
- 单笔确认开票必填校验
- 批量确认开票一致性校验
- 单笔/批量驳回必填校验
- 单笔/批量修改发票必填校验
- 发票详情中个人抬头/企业抬头展示差异
- 历史操作回查开票批次
- 批量上传发票的整票失败与失败明细导出
- 导入成功后“仅能改基础信息，不可改关联订单”的限制

## 12. PRD结论

当前 `beta2` 分支中的发票管理已经不是简单的“发票列表”，而是一个覆盖申请流转、售后约束、批量操作、详情追溯和批量导入的完整工作台。其最重要的产品特征有三点：

- 以订单维度开票申请为主线
- 以状态机和批量一致性校验作为核心控制手段
- 以“批量上传发票”承接 B2B 合并开票场景

如果后续需要，我可以继续把这份文档再往下细化成：

- 面向产品评审的正式 PRD 版本
- 面向研发排期的功能拆解表
- 面向测试执行的测试点/测试用例版本
