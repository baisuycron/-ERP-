# 字段清单模板

## 一、基础信息

- 模块名称：
- 适用页面 / 接口：
- 负责人：
- 更新时间：

## 二、字段清单

| 字段名 | 中文名 | 类型 | 是否必填 | 默认值 | 来源 | 展示规则 | 编辑规则 | 校验规则 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| id | 主键 ID | number | 否 | 系统生成 | 系统 | 不展示 / 只读 | 不可编辑 | 唯一 | 示例 |
| status | 状态 | enum | 是 | draft | 系统 | 文案映射展示 | 特定条件可编辑 | 仅允许枚举值 | 示例 |

## 三、枚举值说明

| 字段名 | 枚举值 | 含义 | 备注 |
| --- | --- | --- | --- |
| status | draft | 草稿 | 默认值 |
| status | enabled | 已启用 | 可用状态 |
| status | disabled | 已停用 | 不可用状态 |

## 四、口径说明

- 同名字段在不同页面是否含义一致：
- 是否存在历史字段兼容：
- 是否有计算逻辑：

## 五、待确认项

- 
